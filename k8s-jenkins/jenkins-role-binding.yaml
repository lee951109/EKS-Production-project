apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: jenkins-admin-binding
subjects:
  - kind: ServiceAccount
    name: jenkins-admin-sa
    namespace: jenkins
roleRef:
  kind: ClusterRole
  name: cluster-admin # 강력 권한 부여
  apiGroup: rbac.authorization.k8s.io